"use strict";

var lapsCompleted = 0; // Create a function that increments the lapsCompleted variable with one
// Run it three times

function incrementsLaps() {
  lapsCompleted = lapsCompleted + 1;
}

incrementsLaps();
incrementsLaps();
incrementsLaps();
console.log(lapsCompleted);