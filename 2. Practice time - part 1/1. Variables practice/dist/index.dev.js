"use strict";

// Create two variables, firstName and lastName
var firstName = 'Filip';
var lastName = 'Rybka'; // Concatenate the two variables into a third variable called fullName

var fullName = firstName + ' ' + lastName; // Log fullName to the console

console.log(fullName);